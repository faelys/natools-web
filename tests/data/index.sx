(elements
   (title Index)
   (main-text
      (tags
         (pages
            "<ul>\n"
            (all-elements
               (render "<li>" (render href) "</li>\n"))
            "</ul>\n<p>Tags:"
            (tags tags (all-children (template
               "\n<a href=\"/tags/" (name (2)) "\">" (name (2)) "</a>")))
            "</p>\n"))
      (load-date "<p>Data loaded on " (in-zone 0 (rfc-3339)) "</p>\n"))
   (sidebar ""))
